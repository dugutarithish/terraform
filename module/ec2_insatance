resource "aws_instance" "example" {
  ami           = var.ami_value # Use underscore, matches your variable
  instance_type = var.instance_type_value
  subnet_id     = var.subnet_id_value

  tags = {
    Name = "terraform_done"
  }
}
